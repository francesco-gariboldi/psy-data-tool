# Printing a message with the number of models generated and confronted
def models_amount_msg(model_formulas):
    print(f"\n\n------------------------------------------------------------------------\n"
          f"Generating and comparing {len(model_formulas)} models...\n"
          f"------------------------------------------------------------------------\n")